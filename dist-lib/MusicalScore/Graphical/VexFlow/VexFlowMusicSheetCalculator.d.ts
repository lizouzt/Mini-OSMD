import { GraphicalMusicSheet } from '../GraphicalMusicSheet';
export declare class VexFlowMusicSheetCalculator {
    private static getKeySignature;
    static format(graphicalSheet: GraphicalMusicSheet, containerWidth?: number): {
        systems: any[][];
        curves: any[];
    };
}
