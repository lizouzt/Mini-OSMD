export class RhythmInstruction {
    constructor(numerator: number, denominator: number) {
        this.numerator = numerator;
        this.denominator = denominator;
    }

    public numerator: number;
    public denominator: number;
}
